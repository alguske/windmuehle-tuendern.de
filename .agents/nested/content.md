# content/ — pages and blog posts

Routing note (kept thin on purpose; the canonical rules live once in the root
`AGENTS.md`). Before working here, read:

- root `AGENTS.md` → "Trilingual Architecture" and "Blog Posts"
- the `new-post` playbook → `.agents/skills/new-post/SKILL.md`

Two reflexes that catch most mistakes in this directory: every change covers
DE + EN + ES, and you run the `i18n-sync` audit plus `zola build` before
committing.
