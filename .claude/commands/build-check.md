# Build Check

Run `zola build` and report any errors or warnings.

Steps:
1. Run `zola build` from the project root
2. If it succeeds, report "Build passed" with a summary of pages generated
3. If it fails, show the errors and suggest fixes

$ARGUMENTS
